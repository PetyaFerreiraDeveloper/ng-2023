import { DateWithHourPipe } from './date-with-hour.pipe';

describe('DateWithHourPipe', () => {
  it('create an instance', () => {
    const pipe = new DateWithHourPipe();
    expect(pipe).toBeTruthy();
  });
});
